{
  "name": "@binyamin/html-tags",
  "version": "0.1.0",
  "description": "A tiny utility for dynamically creating HTML elements",
  "type": "module",
  "main": "out/index.js",
  "types": "./out/index.d.ts",
  "scripts": {
    "build": "esbuild index.js --bundle --minify --tree-shaking --format=esm --platform=browser --outdir=out",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Binyamin Aron Green (https://binyam.in/)",
  "license": "MIT",
  "devDependencies": {
    "aria-attributes": "^2.0.1",
    "esbuild": "^0.14.36",
    "html-element-attributes": "^3.1.0",
    "html-tags": "^3.2.0"
  }
}
